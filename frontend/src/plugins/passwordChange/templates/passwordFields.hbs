<div aria-live="off" class="password-fields">
  <div class="">
    <label for="{{fieldId}}{{genericId}}">{{t 'app.newpassword'}}
      <span aria-label="(required)" class="req">*</span>
      <span id="passwordError{{genericId}}" class="error{{genericId}} field-error feedback"></span>
    </label>
    <div class="password-wrapper">
      <input type="password" class="width-15" id="{{fieldId}}{{genericId}}" name="{{fieldId}}" value="" autocomplete="off"
        aria-describedby="passwordrequirements{{genericId}}">
      <input type="text" class="width-15 display-none" id="passwordText{{genericId}}" value="" autocomplete="off">
      <button aria-label="{{t 'app.revealpassword'}}" href="#" class="toggle-password display-none"><i class="fa fa-eye"></i></button>
    </div>
    <div id="passwordrequirements{{genericId}}">
      <label for="passwordchecklist{{genericId}}" style="position: absolute; left: -10000px; top: auto; width: 1px; height: 1px; overflow: hidden;">
        {{ t 'app.passwordrequirements' }}
      </label>
      <div id="passwordchecklist{{genericId}}" class="password-checklist">
        <p>{{ t 'app.passwordatleast' }}</p>
        <div aria-label="{{ t 'app.passwordneedsspeccialcharacter' }}.">
          <span class="password{{genericId}}-special-char-feedback"><i class="fa fa-times" aria-hidden="true"></i></span>
          {{ t 'app.passwordneedsspeccialcharacter' }}
        </div>
        <div aria-label="{{ t 'app.passwordneedsonenumber' }}.">
          <span class="password{{genericId}}-number-feedback"><i class="fa fa-times" aria-hidden="true"></i></span>
          {{ t 'app.passwordneedsonenumber' }}
        </div>
        <div aria-label="{{ t 'app.passwordneedsuppercaseletter' }}.">
          <span class="password{{genericId}}-uppercase-feedback"><i class="fa fa-times" aria-hidden="true"></i></span>
          {{ t 'app.passwordneedsuppercaseletter' }}
        </div>
        <div aria-label="{{ t 'app.passwordminimumlength' }}.">
          <span class="password{{genericId}}-length-feedback"><i class="fa fa-times" aria-hidden="true"></i></span>
          {{ t 'app.passwordminimumlength' }}
        </div>
        <p class="password{{genericId}}-checklist-feedback"
          aria-live="assertive"
          style="position: absolute; left: -10000px; top: auto; width: 1px; height: 1px; overflow: hidden;"></p>
      </div>
    </div>
  </div>
  <div style="margin-top: 15px" class="">
    <label for="confirmPassword{{genericId}}">{{t 'app.confirmpassword'}}
      <span aria-label="(required)" class="req">*</span>
      <span id="confirmPasswordError{{genericId}}" class="error{{genericId}} field-error feedback"></span>
    </label>
    <div class="password-wrapper">
      <input type="password" class="width-15" id="confirmPassword{{genericId}}" name="confirmPassword" value="" autocomplete="off">
      <input type="text" class="width-15 display-none" id="confirmPasswordText{{genericId}}" value="" autocomplete="off">
      <button aria-label="{{t 'app.revealpassword'}}" href="#" class="toggle-confirmation-password display-none"><i class="fa fa-eye"></i></button>
    </div>
  </div>
</div>